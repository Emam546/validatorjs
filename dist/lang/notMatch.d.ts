import { Validator } from "..";
import { MessagesStore } from "../Rule";
export declare const message: MessagesStore;
export default function UnMatchedType(obj: any, validator: Validator, path: string): string;
